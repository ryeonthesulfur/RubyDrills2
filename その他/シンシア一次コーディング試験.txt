def answer(str)
    num = []
    m = str.chars

    m.each_with_index do |item, index|
        if ((index + 1) % 3 == 0) || ((index + 1).to_s.include?('3'))
            num << item
        end
    end
    return num.join
end

A = gets.chomp
result = answer(A)
puts result