# Ruby 解説ファイル 目次

---

## 変数・オブジェクト

| ファイル | 内容 |
|:---------|:-----|
| [Rubyの変数とオブジェクトの関係（ラベルモデル）](Rubyの変数とオブジェクトの関係（ラベルモデル）.md) | 変数は「名札」、メソッドに渡すと名札だけコピーされる、すべてに同じルールが適用される |

---

## クラス・インスタンス関連

| ファイル | 内容 |
|:---------|:-----|
| [【概説】initializeメソッドとは](【概説】initializeメソッドとは.md) | `.new` 時に自動で呼ばれる初期化メソッドの基本 |
| [クラスメソッドとインスタンスメソッドの違い](クラスメソッドとインスタンスメソッドの違い.md) | `def self.xxx`（クラス）vs `def xxx`（インスタンス）の役割・呼び出し方・エラー原因 |
| [クラスメソッドとインスタンスメソッド_Rails実装例](クラスメソッドとインスタンスメソッド_Rails実装例.md) | `User.all`（クラス）vs `user.save`（インスタンス）の Rails での使われ方・コントローラー例 |
| [クラス設計パターンの解説](クラス設計パターンの解説.md) | 引数リレーで配列を持ち回る設計 → `@@クラス変数` を使う設計への進化 |
| [インスタンス変数とビューの関係解説](インスタンス変数とビューの関係解説.md) | `@変数` がRailsのビューで使える理由（宅配便マジック）、LaravelとのアプローチのちがI |

---

## カプセル化・アクセス権

| ファイル | 内容 |
|:---------|:-----|
| [RubyとPHPのカプセル化の違い](RubyとPHPのカプセル化の違い.md) | Rubyはデフォルトで非公開・`attr_*` で窓口を追加 vs PHPは `public`/`private` を明示宣言 |
| [Rubyのアクセス権とゲッター](Rubyのアクセス権とゲッター.md) | Rubyの `@` はデフォルト非公開、`attr_reader` でゲッター自動生成、`private` はメソッドを隠す用途 |

---

## 文字列操作

| ファイル | 内容 |
|:---------|:-----|
| [Rubyのマイナスインデックス(-1)の解説](Rubyのマイナスインデックス(-1)の解説.md) | `[-1]` は末尾の要素、`[2..-1]` で「3文字目から末尾まで」 |
| [RubyとPHPの文字列操作の比較解説](RubyとPHPの文字列操作の比較解説.md) | `slice` vs `substr`、`* 3` vs `str_repeat` |
| [sliceとend_with?の使い分け解説](sliceとend_withの使い分け解説.md) | 手書きアルゴリズム（`slice`）vs 専用メソッド（`end_with?`）の使い分け |
| [slice!メソッドの解説](slice!メソッドの解説.md) | 破壊的メソッド `!`、元データを変える `slice!` vs 変えない `slice` |
| [indexメソッドの解説](indexメソッドの解説.md) | `str.index("code")` で位置を取得、見つからなければ `nil` |
| [count_code関数の解説](count_code関数の解説.md) | `index` + `if pos` + `+1` で「何文字目か」を出力するパターン |
| [scan_lengthで出現回数を数える](scan_lengthで出現回数を数える.md) | `scan("hi").length` で出現回数カウント（2段階の仕組み） |

---

## 繰り返し・ループ

| ファイル | 内容 |
|:---------|:-----|
| [「each_with_index」とPHP版](「each_with_index」とPHP版.md) | 値とインデックスを同時に取得するループ、PHPの `foreach` との比較 |
| [timesメソッドと「* 3」の違い](<timesメソッドと「* 3」の違い.md>) | `"lo" * 3` は1行出力 vs `3.times` は3行出力 |

---

## ブロック・イテレータ

| ファイル | 内容 |
|:---------|:-----|
| [ブロックと&:の解説](ブロックと&:の解説.md) | ブロックとは何か、`{ }` と `do~end`、`&:メソッド名` の短縮形、`{ }` との使い分け |

---

## その他メソッド・構文

| ファイル | 内容 |
|:---------|:-----|
| [TimeクラスとW記法の解説](TimeクラスとW記法の解説.md) | `Time.new` / `wday` で曜日取得、`%w[...]` で文字列配列を簡単に作る |
| [randメソッドの解説](randメソッドの解説.md) | `rand(n)` は 0〜n-1 の整数、引数なしで小数 |
