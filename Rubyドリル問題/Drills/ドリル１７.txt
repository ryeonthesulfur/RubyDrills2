============================================================
【このファイルで出てくる知識・メソッド・こつ まとめ】
============================================================

■ 手数料込みの残高チェックパターン
  balance >= amount + fee       → 引き落とし額 + 手数料が残高以内かチェック
  balance - amount - fee        → 残高から引き落とし額と手数料を両方引く

■ 重複しない要素だけを合計するパターン
  ネストした each               → 外側でリストを回し、内側で同じ値の個数を数える
  count < 2 なら追加            → 1回しか出てこない数値だけを別配列に集める
  sum = 0 → each で加算        → 集めた配列を合計するループ

■ こつ：重複除外の考え方
  「同じ値が2個以上あれば除外」= 出現回数が1のものだけ合計する
  内側ループで出現回数を数え、2未満なら採用するというロジック

■ PHP 対応メモ
  balance >= amount + fee       → PHP でも同じ書き方で動く
  echo "{$amount}円..." . "\n"  → PHP での文字列出力パターン

============================================================


【Y_Rubyドリル 問題 36 預金システムのアルゴリズム問題】


問題.1
以下の条件を達成するプログラムを実装しなさい

銀行口座に10万円の預金残高があり、お金を引き出すプログラムを作成します。
・お金を引き出すwithdrawメソッドを作成する
・お金を引き出すと手数料110円かかり、「◯◯円引き落としました。残高は◯◯円です」と表示する（残高は手数料を引いた額を表示します）
・もし預金残高より多く引き落としたら「残高不足です」と表示する
・以下にヒントを用意するので参考にしてください


def withdraw(balance, amount)
  fee = 110　　# 手数料
# 引き落とし額と残高を表示する、もしくは残高より多く引き落としたら残高不足と表示
end

balance = 100000　　# 残高
puts "いくら引き落としますか？（手数料110円かかります）"
money = gets.to_i
withdraw(balance, money)



【Ruby版】
def withdraw(balance, amount)
  fee = 110
  if balance >= amount + fee
   puts "#{amount}円引き落としました。残高は#{ balance - amount - fee}"
  else
   puts "残高不足です"
  end
end

balance = 100000
puts "いくら引き落としますか？（手数料110円かかります）"
money = gets.to_i  
withdraw(balance, money)



【PHP版】

function withdraw($balance, $amount) {
    $fee = 110;
    if ($balance >= $amount + $fee) {
        $remaining = $balance - $amount - $fee;
        echo "{$amount}円引き落としました。残高は{$remaining}\n";
    } else {
        echo "残高不足です。\n";
    }
}



----------------------------------------------

【Rubyドリル 問題 37 配列に格納された数値の合計値を算出するプログラムの実装】   難しい

問題.1
概要
本ドリル問題では、配列に格納された3つの数値の合計を算出するプログラムを実装します。

問題
以下の要件を満たすlone_sumメソッドを実装しましょう。

配列に格納された3つの数値の合計を出力する
ただし、同じ数値が2つ以上含まれている場合、その数値は合計する要素に含めない


雛形
def lone_sum(ary)
    # 処理を記述
end

# 呼び出し例
lone_sum([1, 2, 3])
出力例
lone_sum([1, 2, 3]) → 6
lone_sum([3, 2, 3]) → 2
lone_sum([3, 3, 3]) → 0



def lone_sum(ary)
  uniq_nums = []
  ary.each do |num|
    count = 0
    ary.each do |i|
     if num == i
       count += 1
     end
    end

  if count < 2
    uniq_nums << num
  end
end

  sum = 0
  uniq_nums.each do |n|
   sum += n
  end
  puts sum
end


# 呼び出し例
lone_sum([1, 2, 3])
lone_sum([3, 2, 3])
lone_sum([3, 3, 3])




【PHP版】


----------------------------------------------





